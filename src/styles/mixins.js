export const boxShadow = (shadowOpacity, shadowRadius, elevation) => ({
    shadowColor: '#000',
    shadowOffset: {
        height: 1,
        width: 1
    },
    shadowOpacity,
    shadowRadius,
    elevation,
})

export const boxShadowBlue = (shadowOpacity, shadowRadius, elevation) => ({
    shadowColor: '#9EC8FF',
    shadowOffset: {
        height: 1,
        width: 1
    },
    shadowOpacity,
    shadowRadius,
    elevation,
})


